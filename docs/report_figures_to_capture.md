# Figures to Capture for Final Report

Use screenshots from:

- `runs/kpi_batch/retail-shop_FULL_CAM1_vis.mp4`
- `runs/train/yolo_cam1_person/results.png`
- `runs/train/yolo_cam1_person/confusion_matrix.png`
- `runs/train/yolo_cam1_person/BoxPR_curve.png`

## Suggested Figure List

1. System architecture diagram — four-stage pipeline: Video Ingestion and Preprocessing → Detection and Tracking → Identity Resolution → Behavioural Analytics and Reporting (draw manually from §6.1 table).
2. Preprocessing quality report example — screenshot or rendered table of `{video_stem}_preprocess_report.json` showing `total_frames`, `bad_frames`, `bad_pct`, and `reasons` breakdown for one video.  
   *Caption:* Example preprocessing report generated by `preprocess_video()` for a 720-frame, 60.96-second HEVC retail video (2560×1944, 11.81 fps). Eight frames (1.11%) were flagged as bad, all by the low-texture condition (`std_gray ≤ 8`), consistent with brief blank frames during camera initialisation. The remaining 712 frames were passed to the BOTSORT tracker with `frame_is_bad = False`.
3. Sample detection and tracking frame with multiple IDs.
4. Re-entry case frame sequence (before occlusion and after reappearance).
5. Difficult frame example — bad/washed-out frame with `BAD FRAME` overlay from annotated video.
6. YOLO training curves (`results.png`).
7. YOLO confusion matrix.
8. Identity metrics summary table (from JSON/log artifacts).
9. Web dashboard screenshot — clip selected, "Stage 1 · Preprocessing" banner showing 3616 frames / 144 bad (3.98%) · washed out, with per-zone KPI table visible below.

## Suggested Table List

1. Dataset split table (YOLO and ReID datasets).
2. Preprocessing quality audit results table (see §8.1 — video, resolution, fps, frames, bad count, bad %).
3. Detector performance table (precision, recall, mAP).
4. ReID performance table (mAP, Rank-1, Rank-10).
5. CAM1 retest global/per-person metrics.
6. FULL_CAM1 identity consistency metrics (before/after anchor/convergence).
7. Web dashboard API endpoints table (endpoint, method, stage, description): GET /meta/preprocess-report, POST /pipeline/preprocess, POST /pipeline/run, GET /pipeline/status/{job_id}.

